# Base URL of the backend API (include the /api suffix).
# Local development can leave this unset — Vite proxies /api to localhost:5000.
# In production (Vercel), set it to your Render backend URL, for example:
# VITE_API_URL=https://ai-resume-grader-api.onrender.com/api
VITE_API_URL=
